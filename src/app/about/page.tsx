import React from 'react';
import AboutClient from './AboutClient';

export const metadata = {
    title: 'Who We Are | Δωδεκάνησα Glass',
    description: 'The story, vision, and philosophy of Dodecanese Glass.',
};

export default function AboutPage() {
    return <AboutClient />;
}
